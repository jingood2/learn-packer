---
- name: use motd role playbook
  hosts: localhost
  user: ansible
  become: true

  roles:
  - role: motd
    system_manager: admin@golinuxcloud.com

- name: management 'user'
  hosts: localhost
  vars_files: 
    - userlist.yml
  roles:
  - role: usergroup