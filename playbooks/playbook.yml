---
# playbook.yml
- name: 'Provision Image'
  hosts: localhost
  become_user: root
  become_method: sudo
  become: true

  tasks:
    - name: install Apache
      apt:
        name:
        - httpd
        state: latest
        cache_valid_time: 3600